spring:
  datasource:
    url: jdbc:h2:mem:library_test
    initialization-mode: always
    data: classpath:data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true

    show-sql: true
