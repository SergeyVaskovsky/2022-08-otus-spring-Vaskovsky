spring:
  datasource:
    url: jdbc:h2:mem:library

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        format_sql: true

    show-sql: true
